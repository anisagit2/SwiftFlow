{
  "name": "swiftflow-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "deploy": "gcloud run deploy swiftflow-backend --source . --region us-central1 --allow-unauthenticated --project personal-claw-1"
  },
  "dependencies": {
    "@google-cloud/vertexai": "^1.12.0",
    "firebase-admin": "^13.8.0"
  }
}
